/**  */
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  eslint: {
    ignoreDuringBuilds: true,
  },
};

module.exports = nextConfig;

// const nextConfig = {
//   reactStrictMode: true,
//   basePath: "/30"
// }
// module.exports = nextConfig
